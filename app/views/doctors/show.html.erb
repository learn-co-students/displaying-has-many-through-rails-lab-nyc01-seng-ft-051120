<h1> Name: <%= @doctor.name %></h1>
<h3> Department: <%= @doctor.department %></h3>
<h5> Appointments: <% @doctor.appointments.each do |appointment| %>
  <li><%= appointment.appointment_datetime.strftime("%B %d, %Y at %k:%M")%> <%= link_to appointment.patient.name, patient_path(appointment.patient) %></li>
  <%end%>
</h5>